using Gtk 4.0;

template $HypPathBar : ScrolledWindow {
    vscrollbar-policy: never;
    hscrollbar-policy: external;

    Viewport viewport {
        Box segments_box {}
    }

    styles [
        "undershoot-start",
        "undershoot-end",
        "path-bar",
    ]
}
